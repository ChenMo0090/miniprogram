# 2026-04-12 工作日志

## 修复 Java 编译错误

- 修复 `pom.xml`：`maven-compiler-plugin` source/target 从 `15` 改为 `17`，与 Spring Boot 3.x 要求一致
- 修复 `ShopApplication.java`：文本块结束符 `");` → `""");`（Java Text Block 语法错误）
- 错误原因：IDEA 报 "不支持发行版本 5" 是因为 pom.xml source/target 版本与 Project SDK 不一致
- 修复 `Product.java`：`@TableField(exist = false)` 注解误加在方法上，移除即可（该注解只能用于字段）
- 修复 `GroupServiceImpl.java`：`user.getAvatar()` → `user.getAvatarUrl()`，与 User 实体字段名 avatarUrl 一致
- 修复 `AuthInterceptor.java`：补充缺失的静态方法 `getCurrentUserId()` 和 `getCurrentAdminId()`，共修复 6 处调用点
- 修复 `Order.java`：补充缺失字段 productName、productImage、unitPrice、type、payTime、transactionId（同步需在数据库 t_order 表添加对应列）
- 修复 `PageResult.java`：补充带参构造器 `(List, Long, Long)`
- 修复 `OrderServiceImpl.java`：updateStatus 调用参数不足，先查订单取 oldStatus 再传 3 参
- 修复 `PayServiceImpl.java`：Amount 类名冲突改用全限定名；getPackage()→getPackageVal()；getPaidAt→getPayTime；Refund 类型冲突改用全限定名
- 修复 `R.java`：补充 error() 别名方法（等同 fail()）
- 修复 `UserServiceImpl.java`：generateToken 调用多传了 openid 参数，去掉多余参数
- 修复 `pom.xml`：MyBatis-Plus 3.5.6 与 Spring Boot 3.2.x 不兼容，升级至 3.5.9 并改用 mybatis-plus-spring-boot3-starter
- 修复 `PayServiceImpl.java` + `UserServiceImpl.java`：@Value 配置路径错误，统一改为与 application-dev.yml 一致的 shop.wx-pay.* / shop.wx.* 前缀
- 修复 `application-dev.yml`：JDBC URL 添加 `allowPublicKeyRetrieval=true`，解决 MySQL 8.x caching_sha2_password 认证时 "Public Key Retrieval is not allowed" 错误
