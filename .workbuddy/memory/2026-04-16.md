# 2026-04-16 工作日志

## 修复数据库连接问题

- 修复管理后台登录超时问题：
  - 新增 `Admin` 实体类（`shop/admin/entity/Admin.java`）和 `AdminMapper`
  - `AdminController` 补充 `/admin/login` 接口，使用 hutool BCrypt 验密，返回 JWT token
  - 前端 `login/index.vue` 去掉 mock_token fallback，改为真实接口登录
  - `schema.sql` 默认管理员密码改为 `admin123` 的 BCrypt 值，需重新执行 INSERT 或手动更新 t_admin 表

## 修复商品管理空数据 + 图片上传无反应

- **新建 `UploadController`**：`POST /upload`，图片存到本地 `uploads/{年}/{月}/{日}/` 目录，返回 `{ url: "http://..." }`
- **新建 `AppConfig`**：注册 `BCryptPasswordEncoder` 为 Bean
- **新建 `AdminPasswordFixer`**：`ApplicationRunner`，启动时自动检测并修复 `t_admin.password` 格式（从 `$2a$2a$10$...` 修正为 `$2a$10$...`），重置为 `admin123`
- **`WebMvcConfig`** 增加静态资源映射 `/uploads/**` → 本地 `uploads/` 目录；鉴权放行 `/upload`、`/uploads/**`
- **前端 `admin.js`** 修正两处接口路径：
  - `createProduct`: `POST /admin/product` → `POST /admin/product/add`
  - `toggleProductStatus`: `PUT /admin/product/{id}/status` → `PUT /admin/product/{id}/status/{status}`
- **`application-dev.yml`** 添加 `spring.servlet.multipart`（max 10MB）和 `shop.upload.path: uploads`



- 根因：`cn.hutool.crypto.digest.BCrypt.checkpw()` 与标准 BCrypt 哈希不完全兼容，导致验密始终返回 false
- 修复：pom.xml 加入 `spring-security-crypto` 依赖；AdminController 改用 `new BCryptPasswordEncoder().matches()` 验密
- 数据库中已有的标准哈希 `$2a$10$7EqJtq98hPqEX7fNZaFWoOe6s0KBbEz4f...` 无需再改，Spring Security 可正确验证



- 根本原因：后端缺少 `server.servlet.context-path=/api`，Controller 路径 `/admin/login` 未匹配前端代理后的 `/api/admin/login`
- 修复：`application.yml` 添加 `server.servlet.context-path: /api`
- 同步修改 `WebMvcConfig.java` 拦截器路径由 `/api/**` 改为 `/**`，excludePatterns 去掉 `/api` 前缀
- 附带发现 `JAVA_HOME` 指向 JDK 11（`C:\Program Files\Java\jdk-11`），需启动前手动 `$env:JAVA_HOME="C:\Program Files\Java\jdk-17"` 或修改系统环境变量

## 同步更新 `schema.sql`：
  - `t_user.avatar` → `t_user.avatar_url`，与 Java 实体字段 `avatarUrl` 对齐
  - `t_order` 表：`paid_at` → `pay_time`，新增 `product_name`、`product_image`、`unit_price`、`type`、`transaction_id` 字段，与 Java 实体 Order.java 完全对齐
