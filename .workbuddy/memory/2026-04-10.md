# 2026-04-10 工作日志

## 步骤② 完成：商品/订单/支付接口

- 新增/完善文件（共 54 个 Java 文件，全部生成完毕）：
  - 用户模块：LoginDTO / UserInfoDTO / UserService / UserServiceImpl / UserController
  - 商品模块：ProductDTO / ProductCategoryDTO / ProductQueryDTO / ProductService / ProductServiceImpl / ProductController
  - 订单模块：CreateOrderDTO / OrderDTO / OrderQueryDTO / OrderService / OrderServiceImpl / OrderController（Order 实体补全 totalAmount/paidAt/shipCompany/shipNo 字段）
  - 支付模块：PrepayDTO / PayResultDTO / PayService / PayServiceImpl（微信统一下单 + 回调处理 + 退款）/ PayController / PayRecordMapper
  - 退款模块：RefundService / RefundServiceImpl / RefundController（Refund 实体重构对齐字段）
  - 管理后台：AdminController（仪表盘统计 + 商品管理 + 订单管理 + 用户管理 + 退款管理）
  - 工具类：OrderNoUtils 补全 genPayNo / genRefundNo 方法
  - 拼团占位：GroupService 接口（供 OrderServiceImpl 编译依赖，步骤③完整实现）

- 下一步：步骤③ 拼团核心逻辑（待用户确认后开始）

## 步骤③ 完成：拼团核心逻辑

- 新增/修改文件：
  - Group 实体：补全 groupPrice/productName/productImage 快照字段
  - GroupMember 实体：补全 nickname/avatar/role 字段
  - GroupMapper：增加 selectExpiredOngoing/selectByGroupCode
  - GroupMemberMapper：增加 selectByGroupId/selectByGroupIdAndUserId/selectOrderIdsByGroupId
  - GroupJoinDTO / GroupDetailDTO：请求和响应 DTO
  - GroupService 完整接口（发起/参团/成功/失败/我的列表）
  - GroupServiceImpl：核心状态机（发起→参团→满员成团/超时失败→退款）
  - GroupController：发起/参团/详情/我的列表
  - GroupScheduler：
    * 每分钟扫描过期拼团 → handleGroupFail（退款+恢复库存）
    * 每5分钟扫描超30分钟未付款拼团订单 → 自动取消
    * 每天凌晨2点 → 自动确认收货（发货>15天）
  - schema.sql v2.0：全部表结构与实体类对齐（t_group/t_order/t_refund/t_pay_record 字段均已更新）
  - PayRecord 实体：重构对齐
  - WebMvcConfig：白名单增加 /api/group/detail/**
  - MybatisPlusMetaHandler：增加 joinedAt 自动填充

- 下一步：步骤④ 小程序前端页面（待用户确认后开始）

## 步骤④ 完成：小程序前端页面（按设计稿）

- 项目目录：miniprogram/（共 43 个文件）
- 设计稿对应：深紫色 #6C3CE1 渐变体系、金色点缀、绿色退款
- 页面清单：
  - app.js / app.json / app.wxss（全局框架、TabBar、CSS变量）
  - utils/request.js（HTTP封装，自动注入token）
  - utils/util.js（formatPrice/formatDate/formatCountdown/ORDER_STATUS_MAP/GROUP_STATUS_MAP）
  - pages/index（首页：搜索+Banner+分类+商品网格）
  - pages/product（商品详情：价格区渐变/拼团规则卡/规格/购买栏）
  - pages/pay-success（支付成功：拼团进度条+成员头像+倒计时+分享按钮）
  - pages/group-detail（拼团详情：状态机渐变头/成员列表/倒计时/参团/分享）
  - pages/refund（退款：两种模式——拼团成功结果页 + 退款进度时间轴）
  - pages/mine（我的：渐变头/悬浮统计卡/菜单列表/退出登录）
  - pages/order-list（订单列表：Tab筛选/订单卡片/操作按钮）
  - pages/group-list（拼团列表：我的拼团/热门拼团/登录引导）
  - pages/login（登录：全屏渐变背景/品牌/特性说明/一键登录）
- 下一步：步骤⑤ 管理后台（Vue3）或部署配置（待用户确认）

## 步骤⑤ 完成：管理后台（Vue3 + Element Plus）

- 项目目录：shop-admin/（共 18 个文件）
- 技术栈：Vue3 + Vite + Element Plus + Pinia + Vue Router + ECharts
- 设计对应：深色顶部栏 #0D0826、侧边栏 #16113A、金色高亮 #F0B429
- 页面清单：
  - package.json / vite.config.js / index.html（项目配置）
  - src/main.js / App.vue（应用入口）
  - src/router/index.js（路由配置）
  - src/stores/user.js（Pinia 用户状态管理）
  - src/api/request.js（Axios 封装）
  - src/api/admin.js（API 接口定义）
  - src/layouts/MainLayout.vue（主布局：顶部栏+侧边导航）
  - src/views/login/index.vue（登录页：渐变背景+表单验证）
  - src/views/dashboard/index.vue（仪表盘：统计卡片+图表+最近订单/退款）
  - src/views/product/index.vue（商品管理：列表+搜索+新增/编辑/上下架）
  - src/views/order/index.vue（订单管理：列表+筛选+发货+详情）
  - src/views/group/index.vue（拼团管理：列表+进度+成员+详情+关闭）
  - src/views/user/index.vue（用户管理：列表+详情+启用/禁用）
  - src/views/refund/index.vue（退款管理：列表+统计+处理+详情）
- 启动命令：cd shop-admin && npm install && npm run dev
- 下一步：部署配置（Docker Compose 完善 + Nginx 配置）

## 代码检查修复

- 修复 `App.vue` CSS 语法错误：`::root` → `:root`，`::-webkit-scrollbar` 语法修正
- 管理后台代码静态检查完成，所有页面功能完整
